{
  "name": "orb-rush",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pack": "npm run build && cd dist && zip -r ../orb-rush-playable.zip ."
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "stripe": "^22.6.2"
  }
}
